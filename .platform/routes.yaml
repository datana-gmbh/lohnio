"https://{all}/": { type: upstream, upstream: "lohnio:http" }
"https://www.{all}/": {type: redirect, to: "https://{all}/" }
"http://{all}/": { type: redirect, to: "https://{all}/" }
"http://www.{all}/": { type: redirect, to: "https://{all}/" }
